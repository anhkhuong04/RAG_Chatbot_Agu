# Web Framework
fastapi
uvicorn[standard]
python-dotenv
python-multipart

# LlamaIndex Core & OpenAI
llama-index==0.14.16
llama-index-llms-openai==0.6.26
llama-index-embeddings-openai==0.5.2

# Parsing & Vector Store
llama-parse==0.6.54
llama-index-vector-stores-qdrant==0.9.2
qdrant-client

# Re-ranking (Advanced RAG)
sentence-transformers==5.3.0
# Force CPU-only torch to avoid downloading large CUDA runtime packages in Docker
torch==2.5.1+cpu

# Hybrid Search (BM25 + Dense)
llama-index-retrievers-bm25==0.7.0
rank-bm25

# Structured Data Query (Pandas + CSV)
llama-index-experimental==0.6.5
# Required so llama-index experimental modules can import Mistral symbol
mistralai==1.8.2
polars
pandas

# Redis Cache (Optional but recommended)
redis

# Web Search Fallback (Optional - for fallback mechanism)
ddgs

# Document Parsing (Multi-format support)
python-docx
docx2txt
pillow
striprtf

# Utilities
pydantic
pydantic-settings
pymongo

# Authentication (JWT + bcrypt)
python-jose[cryptography]
passlib[bcrypt]
bcrypt>=4.0.1,<4.1