# Web Framework
fastapi
uvicorn[standard]
python-dotenv
python-multipart

# LlamaIndex Core & OpenAI
llama-index
llama-index-llms-openai
llama-index-embeddings-openai

# Parsing & Vector Store
llama-parse
llama-index-vector-stores-qdrant
qdrant-client

# Re-ranking (Advanced RAG)
sentence-transformers
torch

# Hybrid Search (BM25 + Dense)
llama-index-retrievers-bm25
rank-bm25

# Structured Data Query (Pandas + CSV)
llama-index-experimental
pandas

# Redis Cache (Optional but recommended)
redis

# Web Search Fallback (Optional - for fallback mechanism)
ddgs

# Document Parsing (Multi-format support)
python-docx
docx2txt
pillow
striprtf

# Utilities
pydantic
pydantic-settings
pymongo

# Authentication (JWT + bcrypt)
python-jose[cryptography]
passlib[bcrypt]
bcrypt>=4.0.1,<4.1