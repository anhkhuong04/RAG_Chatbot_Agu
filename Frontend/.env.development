# Frontend dev mode (Vite)
# Force frontend to call local uvicorn directly (avoid hitting Docker backend on :8000)
VITE_API_URL=http://127.0.0.1:8012

# If VITE_API_URL is cleared, Vite proxy will use this target
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8012
